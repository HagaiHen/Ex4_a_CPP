#include "Duke.hpp"
#include "Assassin.hpp"

namespace coup {
    
    void coup::Duke::block(Assassin as) {

    }
    
    Duke::Duke (Game g, string s) {
        this->game = g;
        this->s = s;
    }
}