#include <string>
#include <vector>
using namespace std;
#pragma once

namespace coup {

    class Game {
        public:
            // Game ();
            vector<std::string> players();
            std::string turn();
        
    };
    
}