#include "Game.hpp"
#include <vector>
#include <string>

namespace coup {

    std::vector<std::string> coup::Game::players() {
        vector<std::string> vec;
        return vec;
    }
    std::string coup::Game::turn() {
        string s;
        return s;
    }
}